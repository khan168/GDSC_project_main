import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class SingUpButtons extends StatefulWidget {
  const SingUpButtons({Key? key}) : super(key: key);

  @override
  _logInSingUpButtonsState createState() => _logInSingUpButtonsState();
}

class _logInSingUpButtonsState extends State<SingUpButtons> {
  @override
  Widget build(BuildContext context) {
    return SafeArea(child:
          Container(

            child: Column(
          children:[
          Text('hii'),
                ]
      ),
          ),
    );
  }
}
